import { PendingPage } from '../../components/PendingPage';

export default function LabelsPage() {
  return (
    <PendingPage
      active="/labels"
      title="Label Designer"
      blurb="The WYSIWYG label designer (Zint rendering, scannability checks, PDF/PNG/ZPL output) will live here. This page is still pending — we’re building pages one at a time."
    />
  );
}
