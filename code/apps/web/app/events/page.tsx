import { PendingPage } from '../../components/PendingPage';

export default function EventsPage() {
  return (
    <PendingPage
      active="/events"
      title="Events & Trace"
      blurb="The event stream, forward/backward trace explorer and recall fan-out will live here. This page is still pending — we’re building pages one at a time."
    />
  );
}
