import { PendingPage } from '../../components/PendingPage';

export default function MasterDataPage() {
  return (
    <PendingPage
      active="/master-data"
      title="Master Data"
      blurb="Products, GTINs, manufacturers, brand owners and pack hierarchy will live here. This page is still pending — we’re building pages one at a time."
    />
  );
}
